import { describe, it } from "vitest";

// DEPRECATED: This component and test file have been replaced by src/components/flashcards/FlashcardsList.tsx
// Any future tests should be written for that component instead.

describe("FlashcardCard", () => {
  it("is deprecated - use FlashcardsList component instead", () => {
    console.warn("FlashcardCard is deprecated, use FlashcardsList instead");
  });
});
